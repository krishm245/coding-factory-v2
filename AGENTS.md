# Repository Instructions

- Always use `pnpm` for package manager and script commands in this repo.
- Never use `any` in TypeScript code.
- Always use classes for any new service added.
- Always use unit tests for any new feature developed.
